import { FC } from 'react';

import GalleryList from '@/ui/gallery/gallery-list/GalleryList';

const Gallery: FC = () => {
  return (
    <div>
      <GalleryList />
    </div>
  );
};

export default Gallery;
